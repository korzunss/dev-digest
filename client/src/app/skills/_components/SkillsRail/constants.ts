/** Width of the left rail. Matches the agent editor's list so the two screens
    line up when you switch between them. */
export const RAIL_WIDTH = 300;

/** Topbar height — subtracted so the rail and the editor scroll independently. */
export const SHELL_OFFSET = 52;

/** Skeleton cards shown while the list loads. */
export const SKELETON_COUNT = 4;

/** Height of one skeleton card — roughly a real card with its stats footer. */
export const SKELETON_HEIGHT = 128;

/** The tab a card links to when nothing has picked one yet. */
export const DEFAULT_TAB = "config";
